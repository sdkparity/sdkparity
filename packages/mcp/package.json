{
  "name": "@sdkparity/mcp",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src",
    "coverage": "bun test src --coverage --coverage-reporter=text"
  },
  "dependencies": {
    "@sdkparity/openapi": "file:../openapi",
    "zod": "^4.1.13"
  }
}
