---
title: "SDK Surface Manifest"
description: "A compact JSON description of an SDK's public API."
---

An SDK surface manifest records exported classes, methods, functions, models,
enums, signatures, diagnostics, and package metadata.

The manifest is designed for machines first. It lets CLIs, CI jobs, dashboards,
and agents inspect an SDK without reading the whole repository.
